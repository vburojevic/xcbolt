package tui

const (
	// maxConsoleLines caps the in-memory console log buffer during run mode.
	maxConsoleLines = 20000
	// maxStreamTabLines caps the in-memory stream tab log buffer.
	maxStreamTabLines = 20000
	// maxIssues caps the in-memory issues list.
	maxIssues = 2000
)
